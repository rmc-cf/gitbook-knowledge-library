# 随机变量及其分布

### 0-1分布

* 只做1次试验
* E(x) = p，D(x) = p(1-p)

### 二项分布，可以理解就是多×n

* B(n,p)
* n次独立重复的0-1试验
* E(x) = np，D(x) = np(1-p)



### \*泊松分布、指数分布（少见）

* 泊松分布 P(入)：E = D = 入
* 指数分布：E = 1/入 D = 1/(入^2)



### 样本分位数

* 首先要排序
* np不为整数 ⇒  排序后的第np+1个
* 整数 ⇒ (第np+第np+1)/2)
